NHOST_SUBDOMAIN=local
NHOST_REGION=local

# Server-only: copy from Nhost Dashboard → Settings → Secrets (HASURA_GRAPHQL_ADMIN_SECRET).
# Use quotes if the value contains #, $, or spaces.
NHOST_ADMIN_SECRET=""

# Player PWA base URL — used for invite QR codes and share links
NEXT_PUBLIC_CLIENT_APP_URL=http://localhost:3001
